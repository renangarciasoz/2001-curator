# dependencies
/node_modules/

# Next.js build output
/.next/
/out/
/dist/
next-env.d.ts
*.tsbuildinfo

# secrets — never versioned. The key contract lives in .env.example
.env
.env.*
!.env.example

# dataset export artifacts
/exports/

# OS / editor
.DS_Store
*.log

# local agent preferences (a shared settings.json would be versioned)
/.claude/settings.local.json
